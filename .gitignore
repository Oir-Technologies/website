# Design handoff bundle (reference only — never deploy)
_design_extract/
design_response.txt

# OS / editor cruft
.DS_Store
Thumbs.db
*.log

# Local preview tooling
.claude/launch.json

# Internal launch / working docs — keep local, never publish on the public site
OIR_SITE_AUDIT.md
LAUNCH_READINESS_REPORT.md
DEPLOYMENT.md
*_AUDIT.md
*_READINESS_REPORT.md

# Internal sales materials — keep local. This GitHub repo is PUBLIC and the
# whole repo root is published to Netlify, so internal playbooks must stay out
# of both. Back these up to a private location (private repo, Drive, or Notion).
_sales/
